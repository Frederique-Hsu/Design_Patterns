/*!
 *  \file       Row.hpp
 *  \brief
 *
 */


#pragma once


namespace gof_design_patterns
{
    class Row
    {
        /*!
         *  \todo   Glyph subclasses that have children (e.g. Row and Colum) must implement \a next() method,
         *          so that it calls \fn GlyphContext::Next at each point in the traversal.
         */
    };
}
